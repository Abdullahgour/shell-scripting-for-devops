name: check hello
on: 
 push:
   branches: [main]

jobs:

  greet:
  
    steps:
      - name: say hello
        run: echo " hello buddy"
      - name:  hello
        run: echo " how are  buddy"
        
